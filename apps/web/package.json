{
  "name": "@sharaga/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "vite",
    "gen:init-data": "node scripts/generate-dev-init-data.mjs",
    "build": "tsc -p tsconfig.json && vite build",
    "test": "node --test",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@sharaga/contracts": "workspace:*",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.17",
    "@types/react-dom": "^18.3.5",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
